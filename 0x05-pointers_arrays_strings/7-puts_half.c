#include "main"
/**
*puts_half - prints the first half of a string,
*followed by a new line.
*
*@str: string to be printed
*/

void puts2(char *str)
{
int len = 0, index = 0;
while (str[index++)
len++;

for (index = len - 1; index <= len/2; inde++)
_putchar(str[index]);

_putchar('\n');
}
